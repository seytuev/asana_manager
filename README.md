# 🤖 Asana → Telegram Bot

Пересылает уведомления из Asana в Telegram-группу. Готов к деплою на Railway.

---

## 🚀 Деплой на Railway — пошагово

### 1. Загрузи код на GitHub

1. Зайди на [github.com](https://github.com) → **New repository**
2. Назови репозиторий, например `asana-telegram-bot`
3. Загрузи все файлы из этой папки в репозиторий

### 2. Задеплой на Railway

1. Зайди на [railway.app](https://railway.app) → **New Project**
2. Выбери **"Deploy from GitHub repo"**
3. Выбери свой репозиторий `asana-telegram-bot`
4. Railway автоматически найдёт `railway.json` и начнёт деплой

### 3. Заполни переменные окружения

В Railway Dashboard → твой проект → вкладка **Variables** → добавь:

| Переменная | Значение |
|---|---|
| `TELEGRAM_BOT_TOKEN` | Токен от @BotFather |
| `TELEGRAM_CHAT_ID` | Chat ID группы (отрицательное число) |
| `ASANA_ACCESS_TOKEN` | Токен из app.asana.com/0/developer-console |
| `ASANA_PROJECT_GID` | GID проекта из URL Asana |
| `ASANA_WEBHOOK_SECRET` | Любой секрет длиной 32+ символа |
| `PUBLIC_URL` | URL твоего Railway-приложения (см. шаг 4) |
| `NOTIFICATION_LANGUAGE` | `ru` или `en` |

> ⚠️ `PORT` Railway проставит сам — не добавляй его вручную

### 4. Узнай URL приложения

В Railway Dashboard → вкладка **Settings** → раздел **Networking** → **Generate Domain**

Скопируй URL вида `https://asana-bot-production.up.railway.app`
и вставь его в переменную `PUBLIC_URL`.

### 5. Зарегистрируй Webhook в Asana

Открой **Railway Shell** (вкладка в Dashboard) и выполни:

```bash
node src/setup-webhook.js
```

Ты увидишь:
```
✅ Webhook создан! ID: 123456789
```

**Всё готово!** 🎉 Теперь все события из Asana будут приходить в Telegram.

---

## 📬 Какие уведомления приходят

- ➕ Создана задача — название, проект, исполнитель, срок
- ✏️ Задача изменена — что и кем
- ✅ Задача выполнена
- 🗑 Задача удалена
- 💬 Новый комментарий — текст и автор
- 📂 Создана секция
- 📎 Прикреплён файл

---

## 🔧 Как получить нужные данные

### Telegram Chat ID

1. Добавь бота в группу, сделай его **администратором**
2. Отправь любое сообщение в группу
3. Открой в браузере:
   ```
   https://api.telegram.org/bot<ТВОЙ_ТОКЕН>/getUpdates
   ```
4. Найди `"chat":{"id": -XXXXXXXXX}` — это и есть Chat ID

### Asana Project GID

Открой нужный проект. В URL будет:
```
app.asana.com/0/1234567890/...
               ^^^^^^^^^^^ — это GID
```

### Asana Personal Access Token

[app.asana.com/0/developer-console](https://app.asana.com/0/developer-console) → **New access token**

---

## ❓ Частые проблемы

**Бот не пишет в группу**
→ Убедись что бот — администратор группы и `TELEGRAM_CHAT_ID` правильный (отрицательное число)

**Webhook не создаётся**
→ Убедись что Railway-приложение запущено и `PUBLIC_URL` заполнен правильно (без / в конце)

**Нет уведомлений**
→ Проверь логи: Railway Dashboard → вкладка **Deployments** → **View Logs**
